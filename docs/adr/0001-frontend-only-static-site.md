# Frontend-only static site

This is a gift she views, not a shared app. We ship a static frontend (GitHub Pages) with no backend or database. The Editor alone changes content; the Viewer never needs write APIs. Render was rejected to avoid hosting and auth complexity for a single private audience.
