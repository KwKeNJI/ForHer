# Vite + React + TypeScript on GitHub Pages

The app is a static SPA built with Vite, React, and TypeScript, deployed to GitHub Pages. No backend host (Render) is used. Journey state and First Open live in the client. Vite `base` must match the Pages URL shape (user/org site vs project site).
