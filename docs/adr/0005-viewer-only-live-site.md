# Live site is Viewer-only

The Published site never exposes Editor controls (Edit, Remove, add Activity). Content changes happen only via the data file and Publish. Stitch mocks that show those buttons are Editor/design references, not Viewer UI.
