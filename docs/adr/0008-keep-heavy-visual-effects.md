# Keep heavy WebGL / Three.js motion

v1 keeps the rich visual effects from the Stitch mocks (e.g. shader backgrounds, 3D heart) even on mobile. Smoothness is secondary to the wow factor the Editor wants for the gift. Optimize where easy (pause off-screen, lower DPR on small screens) but do not replace these effects with CSS-only motion for v1.
