# Activity images use remote URLs

Activity images are referenced by remote URLs in the data file, not bundled in the repo. Chosen for simpler Publish (no image files to commit). Trade-off: links can break later — the Editor should prefer stable URLs (their own hosting or durable CDNs), not disposable mock links from design tools.
